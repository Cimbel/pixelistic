version: 0.2
phases:
  install:
    runtime-versions:
       nodejs: latest
       python: latest
  pre_build:
    commands:
      - ls -la
      - cd ./pixelapp_fe
      - npm install
  build:
    commands:
      - npm run build && cd ../
      - cd ./pixelapp_be
      - npm install && cd ../
  post_build:
    commands:
      - pwd
      - chmod 700 awscli-workflow.sh
      - ls -la
      - ./awscli-workflow.sh
artifacts:
  files:
     - ./pixelapp_fe/build/**/*
